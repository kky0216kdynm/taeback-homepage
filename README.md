# 태백 생연탄구이 (React)

## 실행
```bash
npm install
npm run dev
```

## 빌드
```bash
npm run build
npm run preview
```

## 메뉴 이미지 교체
`src/assets/menu/` 폴더에 이미지를 넣고 파일명을 유지하면 자동으로 불러옵니다.
(파일명 마지막 '_' 뒤가 카드 제목으로 표시됩니다)
